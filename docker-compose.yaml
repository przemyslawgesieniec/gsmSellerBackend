version: '3.9'

services:
  app:
#    build: .
    image: pgesieniec/gsmseller:0.0.2-amd64
    container_name: gsmseller
    ports:
      - "40157:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    restart: always
